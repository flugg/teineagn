import React from 'react';
import Layout from '../components/Layout';

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>Side ikke funnet</h1>
      <p>Her var det ingen fisk på kroken.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
